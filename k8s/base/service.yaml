apiVersion: v1
kind: Service
metadata:
  name: mermaid-validator-mcp
  labels:
    app: mermaid-validator-mcp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: mermaid-validator-mcp