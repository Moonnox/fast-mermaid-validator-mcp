apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: mermaid-validator-mcp-base

resources:
- deployment.yaml
- service.yaml
- configmap.yaml
- networkpolicy.yaml
- ingress.yaml

commonLabels:
  app: mermaid-validator-mcp
  managed-by: kustomize

images:
- name: icr.io/mjc-cr/mjc-mermaid-validator
  newTag: 1.0.0-amd64